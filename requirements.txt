# veritree_datascience_service -- runtime dependencies
#
# Requires Python 3.10 or 3.11 (see README): tensorflow 2.15.1 ships no wheel above
# cp311, and src/awskit/datastores.py uses PEP 604 `str | None` annotations.

# --- service ---
fastapi==0.115.11
# A range, NOT a pin: litellm pins pydantic exactly (==2.12.5 at litellm 1.83.4) and a
# competing pin here is an install-time conflict. litellm decides the exact version.
pydantic>=2.9,<3
uvicorn==0.34.0
python-multipart==0.0.20
click==8.1.8
python-box==7.3.2
python-dotenv==1.0.1
PyYAML>=5.3.1

# --- data / storage ---
boto3==1.35.46
botocore==1.35.46          # transitive via boto3; pinned to keep the pair in step
SQLAlchemy==2.0.30
psycopg2-binary==2.9.11    # DBAPI driver SQLAlchemy loads by name, never imported
sshtunnel==0.4.0           # only used when AWS_RDS_SSH_HOST is set
paramiko>=3.4,<4
pandas==2.2.1
numpy<2

# --- computer vision ---
pillow>=10.4.0
torch==2.2.2
torchvision==0.17.2        # transitive via ultralytics
ultralytics==8.2.34
opencv-python>=4.1.1,<4.10 # transitive via ultralytics
scipy>=1.4.1,<1.13         # transitive via ultralytics
tqdm>=4.66.3               # transitive via ultralytics, openai
tensorflow==2.15.1
ml_dtypes==0.3.2           # transitive via tensorflow

# --- model providers ---
# anthropic backs content tagging, content moderation and (via litellm) the
# verification_summarization analysis.
anthropic==0.86.0
openai==2.30.0             # transitive via litellm, which pins it exactly
litellm==1.83.4

# --- misc ---
requests==2.32.3

# --- dev / test ---
pytest==9.0.3
