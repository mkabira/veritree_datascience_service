# Optional geospatial extras.
#
# src/libs/geospatial.py is carried over from veritree_multispectral_foresthealth for
# future raster-serving routes, but nothing in the current API imports it. These
# packages pull in GDAL and roughly double the image size, so install them only when
# a route actually needs to open rasters:
#
#   pip install -r requirements.txt -r requirements_geospatial.txt

-r requirements.txt

geopandas==1.1.3
rasterio==1.4.4
geojson==3.2.0
h3==4.4.2
