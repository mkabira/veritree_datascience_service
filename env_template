# Environment Variables Template
# Fill in blanks "<<<>>>" with your credentials
# Then rename this file to ".env"

# --- API ---
API_ENDPOINT_TOKEN = "<<<>>>"

# --- Model providers ---
# ANTHROPIC_API_KEY backs content_tagging and content_moderation (the only backend),
# and is the default for the verification_summarization analysis.
ANTHROPIC_API_KEY = "<<<>>>"
# Only needed if configs/config_cv_verification_summarization.yaml is switched to a
# gpt or gemini model; no computer_vision route uses these.
OPENAI_API_KEY = "<<<>>>"
GOOGLE_AI_STUDIO_API_KEY = "<<<>>>"

# --- Analytics database (datascience_results routes) ---
AWS_RDS_REGION_NAME = "<<<>>>"
AWS_RDS_ENDPOINT = "<<<>>>"
AWS_RDS_PORT = <<<>>>
AWS_RDS_DB = "<<<>>>"
AWS_RDS_USER_NAME = "<<<>>>"
AWS_RDS_PASSWORD = "<<<>>>"

# --- Default AWS credentials ---
# Loaded into the process environment by dotenv.load_dotenv() in src/utils/context.py.
# No os.getenv() call reads these by name: boto3's default credential chain picks them
# up from the environment itself, which is what a bare boto3.Session() resolves against
# when prod_mode is True. Do not remove because a code search finds no reference.
AWS_ACCESS_KEY_ID = "<<<>>>"
AWS_SECRET_ACCESS_KEY = "<<<>>>"
AWS_S3_REGION_NAME = "<<<>>>"
AWS_S3_BUCKET_NAME = "<<<>>>"

# --- Computer vision source bucket (inbound field photos) ---
AWS_SOURCE_ACCESS_KEY_ID = "<<<>>>"
AWS_SOURCE_SECRET_ACCESS_KEY = "<<<>>>"
AWS_SOURCE_REGION_NAME = "<<<>>>"
AWS_SOURCE_S3_BUCKET_NAME = "<<<>>>"

# NOTE: AWS_DESTINATION_* is no longer required. survivability_classifier returns
# bounding boxes as JSON instead of writing an annotated image back to S3, so the
# service has no destination bucket. Remove these from your ECS task definitions.
